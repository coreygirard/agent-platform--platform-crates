[package]
name = "platform-sg-graph-client"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Social-Graph graph-access client for third-party apps: resolve a user's per-app pseudonym, register graph-app membership, and list connections-who-joined, from an opaque graph-access token."

[dependencies]
reqwest.workspace = true
serde = { workspace = true }
serde_json.workspace = true

[dev-dependencies]
tokio = { workspace = true }
wiremock = "0.6"

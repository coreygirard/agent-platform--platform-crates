[package]
name = "platform-drive-deposit"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Least-privilege Drive deposit client for third-party apps: encode a row as a canonical store-core feed-op and POST it into a recipient's partition via their deposit-cap."

[dependencies]
reqwest.workspace = true
serde = { workspace = true }
serde_json.workspace = true
base64.workspace = true
store-core.workspace = true

[dev-dependencies]
tokio = { workspace = true }
